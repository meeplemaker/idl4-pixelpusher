/// <reference types="node" />
import DeviceData from './DeviceData';
export default class Device {
    deviceData: DeviceData;
    readonly stripPacketData: {
        packetIndex: number;
        dataOffset: number;
        rgbaOffset: number;
    }[];
    private packetBuffer;
    private packetIndex;
    private packetSequenceNumber;
    private packetsPerRefresh;
    private minimumUpdatePeriod;
    readonly stripDataSize: number;
    private renderfn;
    private shouldRender;
    constructor(deviceData: DeviceData);
    setRGBABuffer(data: Buffer | Uint8ClampedArray): void;
    sendPacket(packet: Buffer, deviceData: DeviceData): void;
    fullRefreshTick(): void;
    setMaxFPS(maxFps: number): void;
    startRendering(renderfn: () => boolean | void, maxFps?: number): void;
    stopRendering(): void;
}
